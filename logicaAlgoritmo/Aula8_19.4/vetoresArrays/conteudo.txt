Um int representa 4 bytes e vai ocupando o banco de dados

índice da onde o conteúdo se encontra dentro do vetor

todo vetor ou array inicia em 0

vetor = tipo, nome, tamanho em numero de posições
int vetor[5] = {10,20,30,40,50};

acessar alguma posição do vetor
vetor[0] = 10;   

sizeof(vetor) =  ele entrega o numero de bytes do vetor para saber o numero de elementos reais voce teria que dividir pelo valor de cada int que são 4 bytes
se voce chama a função e ela retorna 20, por exemplo, voce divide por 4 que resulta em 5 elementos.
  